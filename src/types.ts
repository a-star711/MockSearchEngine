export interface Suggestion {
  title: string;
}

export interface HistoryItem {
  title: string;
  isHistory?: boolean;
}

export interface DataItem {
  title: string;
  url: string;
  description: string;
}
